int gcd(int u, int v)
{
	if (v == 0)
		return u;
	else
		return gcd(v, u - u / v * v);
}
int main(void)
{
	int x;
	int y;
	x = input();
	y = input();
	output(gcd(x, y));
	if(1)
		if(2)
			output(123);
		else
			output(456);
	//x[1] = 0;

	return 0;
}
